#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "stemmer.h"

#define MAX_LENGTH 100000


int main (void) {
    /*char help[MAX_LENGTH];
    while(scanf("%s", &help) != -1) {
        stem(help, 0, strlen(help));
        printf("%s\n", help);
    }*/
    
}